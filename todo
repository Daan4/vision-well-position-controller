test feedback loop with half-step (or less) set.

benchmark vision algorithm execution time

log some data to csv in debug mode for later plotting

make opencv eccentricity normalized between 0 and 1 somehow like in matlab

potentially add convexity as another feature?

how to do classification? Simple average of values? Or try a k nearest thing
